{
	"version": "0.2.0",
    "configurations": [
        {
            "name": "JSON to GraphQl schema Extension",
            "type": "extensionHost",
            "request": "launch",
            "runtimeExecutable": "${execPath}",
            "args": [
                "--extensionDevelopmentPath=${workspaceFolder}"
            ]
        }
    ]
}